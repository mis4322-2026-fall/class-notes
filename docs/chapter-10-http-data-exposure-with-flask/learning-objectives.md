# Learning Objectives

This chapter stays practical and concept-first. You will focus on:

- Seeing a web app as a data flow system rather than as a large framework.
- Using Flask only as a minimal teaching vehicle for routes, handlers, request data, and templates.
- Keeping examples small enough to trace from browser input to server output.
- Connecting SQLite query results to HTML pages in a way that matches reporting workflows.